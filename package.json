{
  "dependencies": {
    "cheerio": "^1.0.0-rc.12",
    "prettier": "^3.1.1"
  },
  "devDependencies": {
    "@types/bun": "^1.0.0"
  },
  "scripts": {
    "install": "bun build unwebflow.ts --compile; mv unwebflow ~/bin/",
    "test": "bun run unwebflow.ts https://untested.webflow.io"
  }
}